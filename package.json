{
  "name": "css-flexbox-grid",
  "version": "1.0.0",
  "dependencies": {
    "flexboxgrid": "^6.3.1",
    "jquery": "^3.1.1",
    "normalize.css": "^5.0.0"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-watch": "^4.3.11"
  },
  "description": "messing around with grids",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
